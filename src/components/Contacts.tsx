import React, { useState } from "react";

import ContactForm from "./ContactForm";

type Props = {};

const Contacts = () => {
  return (
    <div className="">
      <ContactForm />
    </div>
  );
};

export default Contacts;
